meta {
  name: Submit step (paste)
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/step
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "stepId": "STRIPE_KEY",
    "value": "sk_test_..."
  }
}
