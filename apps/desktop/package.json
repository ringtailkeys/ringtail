{
  "name": "@ringtail/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:dashboard": "cd ../dashboard && bun run build",
    "build:sidecar": "bun run scripts/build-sidecar.ts",
    "prebuild:app": "bun run build:dashboard && bun run build:sidecar",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  },
  "nx": { "tags": ["type:app"] }
}
